const accountId=144553
let accountEmail="anamika@gmail.com"
var accountPassword="12345"
accountCity="Jaipur"
let accountState;//if we dont assign any thing to a variable then it will have value undefined
//to print the variable we can use any of the following syntax
console.log(accountId)
console.table([accountEmail,accountPassword,accountCity])
/*Pefer not to use var because of issue in block scope and functional scope*/ 
//If used postfix, with operator after operand (for example, x++), the increment operator increments and returns the value before incrementing.

//If used prefix, with operator before operand (for example, ++x), the increment operator increments and returns the value after incrementing